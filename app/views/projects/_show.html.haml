.form-view
  = render 'form'

.contextual
  = icon_link_to :new, new_project_activity_path(resource), :remote => true
%h2= t_title :index, Activity

= render 'activities/list', :collection => @project.activities
